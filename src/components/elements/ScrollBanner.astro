---
const { title, items } = Astro.props;
const repeatedItems = [...items, ...items];
---

<div class="overflow-hidden h-30">
  <div
    class="flex whitespace-nowrap animate-scroll-left-slow lg:animate-scroll-left space-x-4"
  >
    {
      repeatedItems.map((item) => (
        <h5
          class="text-transparent  leading-none tracking-tight uppercase
          text-4xl lg:text-9xl inline-block font-medium text-primary py-4 "
          style={{
            WebkitTextStroke: "1px white",
          }}
        >
          {item}
        </h5>
      ))
    }
  </div>
</div>
